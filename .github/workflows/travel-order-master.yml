name: Auto Increment Tag

on:
  push:
    branches: [ "**" ]

jobs:
  increment-tag:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4  # Faz o checkout do código na branch atual

      - name: Get Last Tag and Increment
        run: |
          git fetch --tags

          # Pega a última tag
          LAST_TAG=$(git tag --sort=-v:refname | head -n 1)
          
          # Incrementa a versão da tag
          VERSION_TAG="v$((${LAST_TAG#v}+1))"
          
          echo $VERSION_TAG
          
          # Cria a nova tag
          #git tag $VERSION_TAG
          
          # Empurra a nova tag para o repositório
          #git push origin $VERSION_TAG
          
      - name: Push New Tag
        run: |
          echo "Nova tag criada e empurrada: $VERSION_TAG"